the team size is roughly 10-20 people

Yeah the streamlit app runs inside snowflake as other apple you have built

I think we can store the data inside snwoflake Db as other apps did. Create a new table for it

Same dark theme. 

No Auth required. 

I want the UI to be clean, error free, works perfectly fine. 

Go through all possible erros bugs and have scanrio to handle each scenario.

before you finalise it test it correctly. once done send me ready to copy script here 

