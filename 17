I need you to analyse this Excel workbook and produce a structured plain-text
report for a downstream schema-design task. Output the entire report inside a
single fenced code block. No commentary before or after.

========================================================================
SCOPE
========================================================================
Analyse ONLY these sheets: "Enhancements" and "Automation".
Ignore the "Release" sheet completely — do not profile it, do not sample it.
If either required sheet is missing or renamed, stop and report that fact.

========================================================================
GROUND RULES
========================================================================
- Preserve newlines inside a cell as the literal marker <NL> when quoting.
- Preserve multi-person cells verbatim — do not split names in your output.
- Sample at least 50 rows per sheet when computing distributions, or every
  row if the sheet has fewer than 50.
- Dates: the current file mixes formats. The rebuilt app will enforce
  DD/MMM/YYYY. For any ambiguous cell (e.g. "7/3/2025"), list it by row
  number so a human can disambiguate — DO NOT guess.
- Cell fill colours carry NO consistent meaning in this team. Still list
  every coloured row so we can eyeball them, but do not infer semantics.
- When unsure, write UNCERTAIN and give two candidate interpretations.
- If output gets cut off, I will say "continue from Part N" — resume there.

========================================================================
KNOWN TEAM CONVENTIONS (tell me which of these you observe)
========================================================================
1. Multi-assignee cells use "/" as separator. Max 3 people per cell is the
   target convention. Flag any cell with more than 3.
2. Task Description cells often pack three sub-fields as multi-line text:
     <Title line>
     Why: <reason>
     Where: <platform/system>
   Any of Why/Where may be missing; other labels (How, Impact, What) may
   appear. Parse these out.
3. Status Update cells are rolling logs with dated prefixes like
   "25/06 : text", "10/Apr - text", "09/07/2025 - text". Separators are
   "-" and ":". Mixed date styles.
4. JIRA references sometimes appear inside Task Description or Status
   Update text instead of the JIRA column. Look for PROJ-1234-style tokens
   (letters-dash-digits) and Atlassian URLs.
5. People sometimes sign Status Update entries with initials or first
   names instead of the Assignee being recorded in column B. Extract
   name/initial tokens from Status Update and cross-check.
6. Status values include Queued, In Progress, On Hold, Closed, and
   Cancelled. The rebuilt app treats Cancelled as a valid 5th status —
   do not map it to Closed.

========================================================================
PART 1 — WORKBOOK OVERVIEW
========================================================================
1. Confirm both sheet names: "Enhancements", "Automation". Report actual
   spelling/case found.
2. For each of the two sheets: row count (excluding header), column count,
   whether it looks like active data, archive, or mixed.
3. Column alignment table — do the two sheets share the same columns?
     col_letter | Enhancements header | Automation header | match?
   Flag any mismatch.

========================================================================
PART 2 — PER-SHEET DEEP PROFILE (repeat for Enhancements, then Automation)
========================================================================

=== SHEET: <exact name> ===

HEADERS (row 1, pipe-separated, exact text in column order):
<A> | <B> | <C> | ...

COLUMN PROFILE — one line per column:
<col letter> | <header> | <inferred type> | <% non-blank> | <distinct count> | <min/max if numeric/date> | <up to 5 example values>

MERGED CELLS: <row/col ranges or "none">
HIDDEN ROWS/COLS: <list or "none">
FORMULAS: <column → formula or "none">
DATA VALIDATION / DROPDOWNS: <column → allowed values or "none">

CELL FILL COLOURS (do not infer meaning, just list):
  row | column | colour_hex_or_name | cell_value_excerpt

SECTION-HEADER / SUBTOTAL / BLANK-SEPARATOR ROWS: <row numbers>

SAMPLE ROWS — pick 20 rows covering all of:
 - 1 assignee, 2 assignees, 3 assignees, 4+ assignees (if present)
 - every distinct Status value that appears
 - row with empty Reviewer
 - row with empty Start_Date
 - row with empty % Completed
 - row where Status=Closed but Closed_Date is blank
 - row where % Completed=100 but Status is not Closed
 - row with the longest Status Update
 - row with the shortest non-empty Status Update
 - row with any non-white fill colour
 - oldest Start_Date row, newest Start_Date row
 - row where Task Description clearly contains Why/Where sub-fields
 - row where Task Description is a plain one-liner

Format each sample row as:
  row_number | <A> | <B> | <C> | ... using <NL> for embedded newlines

========================================================================
PART 3 — TASK DESCRIPTION DEEP PARSE (column A, both sheets combined)
========================================================================
The rebuilt app will split this cell into three fields:
  TITLE, BUSINESS_REASON (from "Why:"), PLATFORM_AREA (from "Where:")

1. % of cells that contain a "Why:" label (case-insensitive, also "Reason:",
   "Why -").
2. % of cells that contain a "Where:" label (also "Platform:", "System:").
3. % of cells that are plain one-line titles with no sub-labels.
4. Every distinct sub-label token you find (Why, Where, How, Impact, What,
   When, etc.), with occurrence count.
5. 10 verbatim examples covering the variety (use <NL>). Include at least:
   - full Title + Why + Where
   - Title + Why only
   - Title + Where only
   - plain one-line Title
   - a cell using "Reason:" or other synonym instead of "Why:"
6. Proposed parse for each of the 10 examples:
     TITLE: <...>
     BUSINESS_REASON: <...>
     PLATFORM_AREA: <...>
   Flag UNCERTAIN where the split is not obvious.

========================================================================
PART 4 — STATUS UPDATE DEEP PARSE (column I, both sheets combined)
========================================================================
The rebuilt app will import every dated entry as a separate PROGRESS_NOTES
row. The date prefix (e.g. "25/06 : ") must be STRIPPED from the note body —
only the clean comment text is kept. If an entry has no parseable date
prefix, import it as a single undated note.

1. % of cells with at least one dated entry.
2. Average / max number of entries per non-empty cell.
3. Distinct date-prefix formats with counts:
   DD/MMM, DD/MM, DD/MM/YYYY, MM/DD/YYYY, D/M, YYYY-MM-DD, other.
4. Distinct separators between date and text: "-", ":", other — with counts.
5. % of cells with at least one entry that has no date prefix.
6. Chronological order inside cells: newest-first / oldest-first / mixed —
   give 3 examples of each pattern.
7. 10 verbatim multi-entry examples (use <NL>).
8. For each of those 10, show the proposed parse:
     entry 1: date=<...> | clean_text=<...>
     entry 2: date=<...> | clean_text=<...>
     ...
9. Scan all Status Update cells for JIRA-shaped tokens (letters-dash-digits
   like PROJ-1234) AND for Atlassian URLs. List row numbers and extracted
   tokens — these should populate the JIRA field if the JIRA column is blank.
10. Scan all Status Update cells for name/initial tokens that aren't in the
    Assignee cell for that row. List: row | assignee_cell | tokens_found_in_update.

========================================================================
PART 5 — PEOPLE NORMALISATION (Assignee + Reviewer, both sheets)
========================================================================
A. Raw token list — every distinct person-name after splitting on "/", "&",
   ",", ";", " and ", newline. One line per token:
     <token> | <count> | <sheets> | <columns> | <example row numbers up to 3>

B. Canonical mapping — group tokens that likely refer to the same person:
     CANONICAL: <best guess>
       VARIANTS: <v1>, <v2>, ...
       CONFIDENCE: high / medium / low
       REASON: <why grouped>
   Do NOT silently merge low-confidence matches — list them separately.

C. Multi-assignee analysis:
   1. Cell count by person count: 1, 2, 3, 4+ (flag all 4+ explicitly).
   2. Separator frequency.
   3. Does order seem meaningful? Any evidence of primary vs secondary
      (bold, colour, asterisk, consistent position of the same person)?
   4. 10 verbatim multi-person cells with row numbers.
   5. Any row where the same person appears in BOTH Assignee and Reviewer —
      list row numbers.

The new app models Assignee as: primary (required) + secondary (optional) +
tertiary (optional). Based on what you see, propose a rule for assigning
the 3 slots when the Excel cell has 1, 2, 3, or 4+ names.

========================================================================
PART 6 — STATUS VOCABULARY
========================================================================
1. Every distinct Status value across both sheets, with row count.
2. Map each to ONE of: Queued, In Progress, On Hold, Closed, Cancelled.
   (Cancelled is a valid 5th status — do not fold it into Closed.)
3. Rows where Status is blank — count per sheet.
4. List values that appear in data but are not in the dropdown header.

========================================================================
PART 7 — DATE HYGIENE
========================================================================
For Start_Date and Closed_Date, per sheet:
1. Distinct display formats with counts.
2. Ambiguous rows where format could be US or EU — list row numbers and
   values. Do NOT guess.
3. Rows where the cell is not a real date (e.g. "TBD", "NA", "ongoing").
4. Rows where Closed_Date is before Start_Date.
5. Rows where Status=Closed but Closed_Date is blank — list row numbers.
6. Rows where Closed_Date is set but Status is not Closed — list row numbers.

========================================================================
PART 8 — DATA QUALITY AUDIT
========================================================================
A. Exact duplicate rows: count + row numbers.
B. Same title + same primary assignee (case-insensitive, trimmed): count +
   up to 10 examples.
C. Near-duplicate titles (similarity >= 85%): up to 10 examples with reason.
D. Rows where Assignee is blank: count + row numbers.
E. Rows where % Completed = 100 but Status is not Closed or Cancelled.
F. Rows where Status = Closed but % Completed != 100.
G. Rows where Effort = 0 but Status is In Progress or Closed.
H. Rows where Start_Date is missing.
I. Rows with matching titles or JIRA across Enhancements and Automation —
   list row pairs.

========================================================================
PART 9 — FREE-TEXT FIELD HYGIENE
========================================================================
For Task Description and Status Update, per sheet:
- Max / avg / median character length.
- % of cells with embedded newlines.
- % of cells with bullet markers (-, *, •, →, numbered lists).

========================================================================
PART 10 — CROSS-SHEET RELATIONSHIPS
========================================================================
1. Any column acting as a key linking Enhancements and Automation (Task ID,
   JIRA, title)?
2. Are the two sheets different work types, different lifecycle stages, or
   independent buckets? Justify with evidence.
3. If a task moves between sheets, is it duplicated or moved? Give examples.

========================================================================
PART 11 — RECOMMENDATIONS
========================================================================
List the top 10 concrete issues the rebuilt app must handle that the Excel
does sloppily. Be specific; cite row numbers.

========================================================================
PART 12 — OPEN QUESTIONS FOR THE HUMAN
========================================================================
List every ambiguity you could not resolve. Examples: "Row 16 yellow fill",
"Row 7 has assignee 'Sandesh/Ben/Harr' — is 'Harr' a full name?",
"Start_Date cell on row 42 reads 'TBD'", etc.
